from setuptools import setup

setup(name='image_background_changer_api',
      version='0.0.1',
      description='''This is a deep learning API for changing the background of images
                    to what the user prefers.
                    ''',
      author='Agbleze Linus',
      license='MIT',
      packages=['image_background_changer_api'],
      zip_safe=False
      )